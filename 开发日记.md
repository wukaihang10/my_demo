#

1. gpt提到了可以加入错误重试限制。它的想法是通过对state里某一错误的出现次数进行检查，如果超过阈值就返回失败。  但我觉得有点奇怪，它是针对state.errors这个错误数组进行检查的。 感觉效果一般，我就不实现了。  
2. token budget 也还没有实现。  

## RAG 持久化加载验证层次

实际上存在三种不同的“校验”

不能把所有验证都叫做 validate_manifest。

第一类：格式校验

例如：

manifest.json 是否是 JSON 对象
schema_version 是否是整数
repository_files 是否是字典
每个 sha256 是否是字符串
chunk_count 是否是非负整数

这是：

索引文件能否被正确解释。

应该完全放在：

RAGIndexStorage.load()
第二类：内部完整性校验

例如：

manifest.chunk_count == len(chunks)
vectors.shape[0] == len(chunks)
vectors.shape[1] == manifest.vector_dimension
chunks.json 的哈希是否匹配
vectors.npy 的哈希是否匹配
向量是否包含 NaN 或 infinity

这是：

磁盘上的几个索引文件彼此是否自洽。

也应该完全放在：

RAGIndexStorage.load()

因为它不需要知道当前程序正在使用哪个仓库、哪个模型。

第三类：运行时兼容性校验

例如：

索引里的仓库路径
是否等于当前正在分析的仓库

索引使用的 Embedding 模型
是否等于当前 EmbeddingClient

索引使用的 Chunker 参数
是否等于当前 PythonASTChunker

索引保存的源码快照
是否等于当前仓库源码

这是：

一个本身完整合法的旧索引，是否适合当前这次运行。

它不应该由通用的 RAGIndexStorage 决定。

因为 RAGIndexStorage 只知道：

怎样保存和恢复索引

它不应该知道：

当前 PythonRepositoryRAG 使用什么模型
当前仓库在哪里
当前 Chunker 参数是什么
当前源码是否改变

## RAG关键组件职责

RepositoryKnowledgeManager
    负责：工具参数、ToolResult、当前活动仓库、异常转成工具错误
                      │
                      ▼
PythonRepositoryRAG.ensure_index()
    负责：判断磁盘索引、加载、兼容性检查、失败后重建、保存
                      │
                      ▼
RAGIndexStorage
    负责：文件读写、格式校验、哈希校验

## RAG开发架构

CodeTokenizer
└── 把代码、标识符和查询切分为词项

InMemoryBM25Index
├── 保存 Chunk
├── 建立倒排索引
└── 计算 BM25 分数

BM25Retriever
└── 把查询交给 BM25Index

VectorRetriever
└── 保持现有向量检索

HybridRetriever
├── 同时调用 VectorRetriever
├── 同时调用 BM25Retriever
└── 使用 RRF 融合排名

PythonRepositoryRAG
├── 维护两套检索索引
├── 重建或加载后同步 BM25
└── 对外仍然只提供 search()/answer()

RepositoryKnowledgeManager
└── 完全不知道 BM25、向量和 RRF 的内部实现  
